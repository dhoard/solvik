package removedpublic

// The `public` visibility keyword was replaced by `pub`. `public` is no
// longer accepted as a Solvik keyword.

struct Main {

    public func run(args: String...): Integer {
        return 0
    }
}
