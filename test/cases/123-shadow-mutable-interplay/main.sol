package shadow
struct Main {
    public func run(args: String...): Integer {
        let m: Long = 1
        let mutable m: Long = 2
        m = 3
        System.getOut().println(m)
        return 0
    }
}
